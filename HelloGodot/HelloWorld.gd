extends Node

func _ready():
	var greeting = "Hello"
	var number = 2
	print(greeting + " World! You're #" + str(number) + "!")
	
	# Exercise section 1.4 
	var wait = "What are we waiting for?"
	var thing = "GODOT"
	print (wait + " " + thing + "!")
